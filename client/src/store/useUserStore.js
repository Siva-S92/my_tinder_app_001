import {create} from 'zustand'

export const useUserStore = create((set) => ({
    loading: false,
    
    updateProfile: async (data) => {
        try {
            
        } catch (error) {
            
        }
    },
}))
